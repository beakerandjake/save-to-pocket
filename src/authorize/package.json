{
  "name": "authorize",
  "version": "1.0.0",
  "description": "Lambda authorizer which performs basic HTTP auth against the user Table",
  "main": "index.js",
  "type": "module",
  "license": "GPL-3.0",
  "scripts": {
    "test": "sam local invoke -t ../../template.yml -n ../../local-env.json -e ../../events/authorize.json AuthorizeFunction"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0"
  }
}
